# App
BASE_URL=https://www.bgembassy-cairo.com
DEFAULT_LOCALE=bg
PORT=3000

# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

# Auth (staff)
AUTH_SECRET=change_me_long_random
STAFF_SESSION_DAYS=7

# Email (dev SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
MAIL_FROM="Consular Service <no-reply@bgembassy-cairo.com>"

# Security / rate limiting
RATE_LIMIT_WINDOW_SEC=60
RATE_LIMIT_MAX_REQ=30

# Tokens
BOOKING_TOKEN_TTL_HOURS=168
